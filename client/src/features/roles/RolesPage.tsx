import { ComingSoon } from '@/components/shared/ComingSoon'

export function RolesPage() {
  return (
    <ComingSoon
      title="Roles & Permissions"
      description="Create custom roles and choose which permissions each one grants. The API is already live - this UI is next."
    />
  )
}
