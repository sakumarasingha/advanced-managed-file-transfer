import { ComingSoon } from '@/components/shared/ComingSoon'

export function UsersPage() {
  return (
    <ComingSoon
      title="Users"
      description="Invite users, activate/deactivate accounts and assign roles. The API is already live - this UI is next."
    />
  )
}
